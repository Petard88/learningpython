glossary = {
    'dictionary': 'used to store key-value pairs',
    'string': 'a line of text or numbers',
    'integer': 'numbers',
    'for loop': 'repeats an action for each value in a list or something',
    'if loop': 'does something if the value matches'
    }

print(f"A dictionary is {glossary['dictionary']}. \n\nA string is {glossary['string']}."
      f"\n\nAn integer is {glossary['integer']}. \n\nA for loop is a line that {glossary['for loop']}."
      f"\n\nAn if loop is a line that {glossary['if loop']}.")